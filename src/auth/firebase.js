// Firebase is disabled — all data is managed locally via localStorage
// This file is kept as a stub to avoid import errors

export const auth = null;
export const googleProvider = null;
export const db = null;
export default null;
